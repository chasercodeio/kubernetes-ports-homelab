apiVersion: v1
kind: Service
metadata:
  name: metube-svc
  namespace: homelab
  labels:
    name: metube-svc
  annotations:
    name: metube-svc
spec:
  type: ClusterIP
  ports:
    - port: 8081
      name: http
      targetPort: http
      protocol: TCP
      # nodePort: 30000
  selector:
    name: metube
